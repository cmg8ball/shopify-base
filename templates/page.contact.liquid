<div class="layout">

  {% if page.title != blank %}<h1>{{ page.title }}</h1>{% endif %}

  {% if page.content != blank %}
    <div class="rte">{{ page.content }}</div>
  {% endif %}

  this is cool

  {% render 'form-contact', show_phone: false %}

</div>

{% section 'features' %}